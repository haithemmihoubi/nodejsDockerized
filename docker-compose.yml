version: "3.3"
services:
# app name
  nodejs-app:
  # container name when running and building
    container_name: node-app-container
    build: .
    ports:
      - "3000:3000"
      - "3001:3001"
    env_file:
      - ./.env


